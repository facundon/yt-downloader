import "./index.scss"

type LoginFrameProps = {
   title: React.ReactElement
}

const LoginFrame: React.FC<LoginFrameProps> = ({ title }) => {
   return <div className="container"></div>
}

export default LoginFrame
